import express from 'express';
import authenticateToken from './middleware/authenticateToken'; // Adjust the path to the actual location of your middleware

const router = express.Router();


export default router;
